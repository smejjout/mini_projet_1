#!/usr/bin/env python3

# Management of public transit tickets
# Modeling a ticket buying system

def main_buying(vehicle: str,
                age: int,
                class_nb: int = 0,
                is_one_way: bool = True) -> float:
    """Main function managing the input and redirecting to the sub-function
    Parameters
    ----------
    vehicle : str
        Type of transport, either "bus", "train", or "subway"

    age : int
        Age of the passenger

    class_nb: int
        In the case of train transportation, you have to specify the class, either 1 or 2
        Defaults to 0 which means it's irrelevant (e.g. in a bus)

    is_one_way: bool
        Whether the ticket is a one way ticket or include the return trip
        Defaults to True

    Returns
    -------
    float
        The money you'll have to pay in [€]
    """
    # call age_classification
    # check the vehicle and redirect to sub-function
    # return the result

    pass


def age_classification(age: int):
	"""Classify age
	Parameters
	----------
	age: int
		Age of the passenger

	Returns
	-------
	str
		Age class (e.g. senior, youth, kid)
	"""
	pass


def train(age_class: str,
          class_nb: int,
          is_one_way: bool) -> float:
    """Sub-function managing train tickets
    Parameters
    ----------
    age: int
        Age of the passenger
    class_nb: int
        Class of the ticket
    is_one_way: bool
        Whether the ticket is a one way ticket or include the return trip

    Returns
    -------
    float
        The price of the ticket in [€]
    """
    pass


def bus(age_class: str,
        is_one_way: bool) -> float:
    """Sub-function managing bus tickets
    Parameters
    ----------
    age: int
        Age of the passenger
    is_one_way: bool
        Whether the ticket is a one way ticket or include the return trip

    Returns
    -------
    float
        The price of a ticket in [€]
    """
    pass


def subway(age_class: str) -> float:
    """Sub-function managing subway tickets
    Parameters
    ----------
    age: int
        Age of the passenger

    Returns
    -------
    float
        The price of a ticket in [€]
    """
    pass


